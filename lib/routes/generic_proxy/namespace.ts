import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'Generic Proxy',
    description: 'A utility route that proxies arbitrary HTTP/HTTPS resources. Useful for accessing files from remote servers through RSSHub.',
    categories: ['other'],
    lang: 'en',
};
